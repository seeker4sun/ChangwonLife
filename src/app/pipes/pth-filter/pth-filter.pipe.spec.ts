import { PthFilterPipe } from './pth-filter.pipe';

describe('PthFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
