import { PthLimitToPipe } from './pth-limit-to.pipe';

describe('PthLimitToPipe', () => {
  it('create an instance', () => {
    const pipe = new PthLimitToPipe();
    expect(pipe).toBeTruthy();
  });
});
