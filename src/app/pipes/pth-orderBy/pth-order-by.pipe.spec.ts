import { PthOrderByPipe } from './pth-order-by.pipe';

describe('PthOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new PthOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
